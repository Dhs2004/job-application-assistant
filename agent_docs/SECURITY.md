# Security

- 简历、数据库和投递记录只写入 `.data/`；该目录不得提交。
- SMTP 密钥只从服务端环境读取，不得进入 API 响应、日志或数据库。
- 上传文件必须同时校验扩展名、MIME、大小和解码后内容。
- 岗位 Feed 仅允许 HTTPS，并拒绝本机、私有网段和链路本地目标。
- 自动发送必须经过 SMTP 测试、模板确认、显式开关、阈值、限额和去重检查。
- 日志不得包含简历正文、完整邮件正文、SMTP 密钥或未脱敏邮箱。
