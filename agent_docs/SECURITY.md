# Security

- 简历、数据库和投递记录只写入 `.data/`；该目录不得提交。
- AI API Key 和 SMTP 密码只保存在服务端内存会话中，不得进入 API 响应、日志、数据库或浏览器存储。
- 上传文件必须同时校验扩展名、MIME、大小和解码后内容。
- 自定义 AI Provider 仅允许公开 HTTPS 地址，并拒绝本机、私有网段和链路本地目标。
- 投递前必须验证公开邮箱来源、SMTP 会话、去重键和一次性确认 token。token 必须与岗位、收件人、主题、正文和附件绑定。
- 日志不得包含简历正文、完整邮件正文、SMTP 密钥或未脱敏邮箱。
